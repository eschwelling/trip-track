#!/usr/bin/env ruby
ENV["RAILS_ENV"] ||= ENV["RACK_ENV"] || "development"
ENV["NODE_ENV"] ||= "development"

require "pathname"
ENV["BUNDLE_GEMFILE"] ||= File.expand_path("../Gemfile", Pathname.new(__FILE__).realpath.dirname)

require "rubygems"
require "bundler/setup"

require "vite_ruby"
require "dry/cli"

Dry::CLI(ViteRuby::CLI).call
